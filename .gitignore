# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]

# C extensions
*.so

.vscode/

# Distribution / packaging
.Python
.venv/
venv/
env/
build/
develop-eggs/
downloads/
eggs/
lib/
lib64/
parts/
sdist/
dist
var/
*.egg-info/
.installed.cfg
*.egg
*.pyc

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml

# Translations
*.mo
*.pot

# Django stuff:
*.log

# Sphinx documentation
docs/_build/

# PyBuilder
target/

#Mac
.DS_Store
.AppleDouble
.LSOverride

# local runtime artifacts (gunicorn control socket, pidfiles — git can't
# track sockets anyway, but this documents intent + covers *.pid variants)
*.ctl
*.pid
*.sock

#D2J Error file
classes-error.zip

#IntelliJ IDE
.idea
*/migrations/*
.fuse_hidden*
.tox
.python-version

#MobInspect Files
mobinspect/debug.log
mobinspect/uploads/
mobinspect/db.sqlite3
mobinspect/secret
mobinspect/MobInspect/windows_vm_priv_key.asc
mobinspect/setup_done.txt
TODO.md

# Genymotion installer + runtime (retired — AVD is used instead)
emulator/

# Launcher logs
.start.log
.genymotion.log
.detached_start.sh

# Local PostgreSQL credentials (copy .env.postgres.example → .env.postgres)
.env.postgres
test_files/.e2e_scratch/
tests_e2e/api/.scratch/
