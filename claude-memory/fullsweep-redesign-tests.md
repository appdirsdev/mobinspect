---
name: fullsweep-redesign-tests
description: "IN-FLIGHT (2026-07-09) full-sweep on feature/granite-llm-integration — creative redesign of all 45 templates via workflow, then Playwright UI + backend test coverage; state, infra, how to resume"
metadata: 
  node_type: memory
  type: project
  originSessionId: 67019f0b-e557-4966-8b9c-683562465919
---

User ran **ultracode + fan-out (no-ask)** for a 3-phase sweep on `feature/granite-llm-integration`, 2026-07-09. Builds on [[ai-integration-implemented]].

**PHASE 1 — creative/interactive redesign of ALL pages + component partials (workflow).**
- Target = **45 templates** (list: `$CLAUDE_JOB_DIR/tmp/targets_final.txt`; baselines copied to `$CLAUDE_JOB_DIR/tmp/baseline/<repo-path>` for diff-verify). EXCLUDED: `general/home.html` + `rbac/adb_connections.html` (already done this session with the new creative style), `_ai_analysis.html` (security-critical, keep no-`|safe` text-node render), `_integration_status.html` (trivial), base/ shells + global components/ chrome.
- Workflow `creative-redesign-sweep` (run `wf_0b4d9e22-7dc`, script under session `workflows/scripts/`): pipeline per file → design agent (`ui-ux-designer`) edits in place → verify agent (`general-purpose`) runs `diff -u baseline current` and adversarially confirms functionality preserved. Returns `{verify_failures[], js_changed[], all[]}`. NOTE: first launch failed because `args` arrives as a JSON **string** — script now does `typeof args==='string'?JSON.parse(args):args`. Reference for agents = `general/home.html` (ambient glow hero, Alpine `counter()` count-up, glow/gradient tiles).
- **Hard rule enforced on every agent:** preserve 100% functionality — existing `<script>` byte-identical (new presentational JS may be appended), chart datasets/labels/type untouched (only color/animation/tooltip STYLING), all `{{}}/{%%}`/`{% url %}`/csrf/form field name+id/DataTable/EventSource/Alpine/CodeMirror hooks intact, no new `|safe`, edit only own file.
- **POST-WORKFLOW (my checklist):** rebuild Tailwind `npx -y tailwindcss@3 -c tailwind.config.js -i mobsf/static/mobinspect/css/src/app.css -o .../dist/app.css --minify`; restart server; `manage.py check`; compile-check every template (import `mobsf/StaticAnalyzer/views/android/static_analyzer.py` first so the `|key` filter registers); diff `<script>` blocks vs baseline; live-render pages that have data (3 Android scans in RecentScansDB); fix any verify_failures.

**PHASE 2 — Playwright UI tests, max honest coverage.** No harness exists yet (no package.json/playwright.config/e2e dir). `playwright` on PATH is the **Python** pyenv shim; `node`/`npx` available. Plan: set up harness (prefer `pytest-playwright` to match the Python/pytest repo, else node). Cover every reachable page + key flows (login admin/admin, upload, integrations 4-box save/test, RBAC, recent, report tabs). Honest gap: dynamic-analysis live flows need a real device/emulator (not exercisable headless).

**PHASE 3 — backend tests, max honest coverage.** Runner `scripts/run-tests.sh` (PYTEST=`poetry run pytest`, checks running Postgres). Config in `pyproject.toml [tool.pytest.ini_options]` + `.coveragerc` + `tox.ini`. 54 existing test files, convention `test_cov_*.py`. Per [[coverage-campaign]] the real ceiling was **66.9%** (device/Windows/network-locked paths) — **100% is physically impossible here; report the true number, never fake it.** New code to cover: `llm/{client,prompts,tasks,signals,views}.py`, RBAC `device_save/model_save/*_test_key` + `ModelIntegration.role`, home `index` stats.

**RESULTS (2026-07-09):**
- **Phase 1 DONE:** all 45 templates redesigned (workflow wf_0b4d9e22 + 2-file fixup wf_f9a5f1d4). 49 template files, +16.1k/−6.2k. All compile; JS/hooks/chart-data preserved (deterministic `<script>`-diff + adversarial verify). Fixed 1 real regression (Droidmon `call|key:"method"` dropped in android `dynamic_report.html` — restored as `mi-kv-title`). Cold-shell filter artifacts (`|key`, `base64_decode`) confirmed non-issues (register at runtime via view import). CSS rebuilt (npx tailwindcss@3, now ~57KB). Live-render 200 clean: home, recent, analytics, integrations, dynamic, all RBAC, android report, appsec dashboard, ai_dashboard.
- **Phase 3 DONE (backend, workflow wf_d1624355 + 1 boost agent):** LLM package (client/prompts/tasks/signals/views) **0%→100%**; RBAC `views.py`+`models.py` **→100%** (91.2% RBAC overall); `home.py` 82.3%. ~494 new tests, ALL PASS. Files: `StaticAnalyzer/views/common/llm/test_cov_{prompts,client,tasks,views,signals}.py`, `RBAC/tests/test_cov_integrations.py` + `test_cov_integrations_extra.py`, `MobSF/views/test_cov_home_stats.py`. Fixed 1 redesign-broken existing test (`test_cov_views.py::test_adb_connections_list_ok` — asserted removed listing label; now asserts card + prefilled host). **Fixed a real bug:** URL shadow in `RBAC/urls.py` — `model/<slug:role>/test/` shadowed `model/<int:integ_id>/test/` (slug matches digits); reordered int-routes-first.
- **Phase 2 DONE (Playwright UI, workflow wf_d2895023):** 6 spec files (dashboard/integrations/rbac/analytics/report/general_dynamic) + smoke = **53 tests, all pass** serial (35s). harness stood up at `tests_ui/` (conftest `admin_page` fixture = logged-in page via session `storage_state`; needs pytest-playwright — `poetry run pip install pytest-playwright playwright`; chromium browsers already cached). Smoke test green. 6 spec agents authoring test_{dashboard,integrations,rbac,analytics,report,general_dynamic}.py against the running server. Run UI suite: `poetry run pytest tests_ui/ --browser chromium -q -o addopts=""`. Device/emulator dynamic flows can't run headless (honest gap).

**PHASE 4 — sidebar/header/footer chrome redesign + logo, 2026-07-09 (later session):** `components/sidebar.html`+`sidebar_item.html`+`topbar.html` redesigned with animated active-indicator (gradient bar + icon spring), section labels (Workspace/Administration), gradient-ring avatars, glowing brand mark; footer got a pulsing "Local · Offline-ready" status dot. New CSS in base/app.html `#mi-chrome`. **Logo redesigned**: replaced busy shield+magnifier+check mark with a single geometric **M monogram** (dipped center vertex) on the blue gradient tile (`#3C82F6→#1D4ED8`) — applied to `components/logo_mark.html`, `static/mobinspect/img/{favicon,logo}.svg`. Verified via Playwright (53/53 still pass) + Django test client. Brand showcase artifact published.

**PHASE 5 — "dashboard-design" reference folder → v3 harmonization, 2026-07-09 (still later session):** User dropped 7 reference screenshots (repo root `dashboard-design/`) of a premium dark cybersecurity dashboard called "CyberGuard" (palette `#FE4A23` orange / `#8D5CFC` purple / near-black `#222222`; ambient orange glow atmosphere; icon-chip stat cards; pill-delta badges; several distinctive chart types — 270° radial gauge, dual-wave gradient area, candlestick, dot-matrix bar, compliance color-rows, flow ribbon, incident timeline). Decision: **adopt the craft/patterns, keep MobInspect's own blue brand** (not a rebrand — the M-monogram logo from Phase 4 stays). Built new reusable primitives:
  - `components/gauge_arc.html` — 270° arc gauge, pure CSS/SVG (no JS chart lib). Static tick marks at 0/25/50/75/100 (hardcoded literal coords, precomputed by hand — ticks don't depend on value so no template trig needed). Pointer dot uses TWO composed CSS `rotate()` transforms (outer static 225deg + inner dynamic `{% widthratio value 100 270 %}deg`) to land exactly on the arc's end with zero backend/view changes — pure Django built-in `{% widthratio %}` arithmetic. Progress arc via the existing pathLength=100 + stroke-dasharray trick (`{% widthratio value 100 75 %} 100`, arc rotated 135deg so it opens at 6 o'clock). Params: `value`,`color`,`sub_label`,`size_class`,`animate`. Geometry double-derived + cross-checked at 4 sample values (21/45/58/83), rendered via Django `render_to_string` and published as an artifact for visual proof (browser tool unavailable in this env).
  - New global CSS in base/app.html `#mi-chrome`: `.mi-gauge-*` (pointer/tick/ring), `.mi-pill-delta` (compact trend badge, up/down, theme-aware), `.btn-primary::before` subtle SVG-fractal-noise grain texture (opacity .05, mix-blend-mode overlay — applies to EVERY primary button app-wide automatically, no template changes needed).
  - `static_analysis/appsec_dashboard.html` = flagship: security-score full-circle ring replaced with the new gauge_arc (reused existing `--mi-score-accent` tier-color logic verbatim); old `.mi-score-ring`/`.mi-score-glow` dead CSS removed, print stylesheet updated to target new classes.
  - `chart-theme.js` (global Chart.js defaults): tooltip corner-radius 6→10, padding 10→12, bold title — floating-pill tooltip feel across every chart app-wide.
  - Fan-out workflow `v3-harmonize-sweep` (47 files, baseline+verify same rigor as Phase 1) applies the gauge/pill primitives ONLY where a real 0-100 metric or real delta exists (explicitly instructed: never fabricate data, most pages legitimately get zero changes — framed as harmonization not rebuild, since all pages already got a full creative pass in Phase 1).
  - LLM gap-audit workflow (task wizpddhhv) was STOPPED PARTWAY per user instruction ("stop gap analysis") after hitting a session-limit mid-verify: 69 raw findings, 3 refuted, **30 survived** (0 critical, 5 high, 16 medium, 8 low, 1 info) but report/critic synthesis never ran (null). Full findings list is in the task output if resumed later — not currently actioned.

**PHASE 6 — literal color/chart incorporation per explicit user correction, 2026-07-09 (same day, later):** user overrode "keep blue only" — explicitly wants the reference's ACTUAL colors (`#FE4A23` amber, `#8D5CFC` violet) incorporated into charts/data-viz, "use all the layout style graphs charts as it is," "use our data" (no fake/mock data — reconfirmed hard rule), "enhance charts/layout according to the theme exactly." Response: added `chart.amber`/`chart.violet` as RESERVED data-viz-only tokens in `tailwind.config.js` (never UI chrome/buttons/nav — those stay on the `mobinspect` blue brand + logo, a separate already-approved deliverable). Applied:
  - **4 report pages** (`android_binary_analysis`, `ios_binary_analysis`, `android_source_analysis`, `ios_source_analysis`) — "App scores" card's security_score badge replaced with the `gauge_arc` component + a "Security score" caption label (added after a real Playwright regression: the gauge splits number/"/100" into separate DOM nodes, broke `tests_ui/test_report.py`'s old single-text-node regex — fixed by asserting the new structure instead of forcing ugly concatenation). Deliberately did NOT touch `compare.html`/`compare_versions.html`/`source_tree.html`/`windows_binary_analysis.html` — verified via grep + view source there is genuinely no score/delta data on any of them; forcing a gauge would fabricate data.
  - **New component `components/dot_matrix.html`** — the reference's distinctive dot-grid "skyline" chart, real 14-day daily scan-count data. Analytics view (`Analytics/views.py`) gained additive context: `trend_recent_pairs` (zip of label+value — **Django template dot-lookup cannot index a list by a loop variable**, learned by testing: `{{ list.some_var }}` treats `some_var` as a literal string key, not a variable to resolve, so it silently fails to `''`; the fix is zipping into (label,value) tuples in Python and unpacking via `{% for label, value in pairs %}`) + `trend_recent_max`. **Second real bug caught by testing**: `{% widthratio %}` returns a STRING; comparing it directly against integer `forloop.counter` in `{% if %}` silently evaluates every comparison to False (type-mismatch swallowed, not raised) — fixed with `{{ filled|add:0 }}` to force real int coercion. Both bugs found via `render_to_string()` unit-testing in a Django shell BEFORE wiring into the live page — reinforces: hand-verify any template arithmetic/logic component with real rendered output, never trust it compiles=correct.
  - Analytics `trend_chart` (Chart.js line/area, single real series) recolored blue→amber (`#FE4A23`, styling only). Analytics `platform_chart` donut + its legend `{% cycle %}` recolored to the amber/violet+tints family — legitimate because platform breakdown is **categorical, not severity-semantic** (unlike `severity_chart`, which correctly stays on red/orange/blue/green semantic severity tokens — those must never change, a hard pre-existing rule: "Severity — semantic only. Never use for non-severity UI").
  - Verified: `django manage.py check` clean, all 47 v3-target templates compile, full Playwright UI suite 53/53 pass, `Analytics/tests/` 3/3 pass, real DB render shows honest low counts (no inflation).
  - Explicitly declined (documented, not silently skipped): **candlestick-style chart** — no OHLC-shaped data exists anywhere in MobInspect (would require fabrication); **compliance-style color-rows** — no compliance-percentage dataset exists to back it (OWASP/MASVS mapping isn't currently modeled as scored %).

**PHASE 7 — theme-default fix + Behance case-study deep-dive + Albert Sans, 2026-07-09 (same day, later):** User said "dashboard still looks the same" — root cause found: MobInspect defaulted to `system` theme, but the reference is dark-only; a light-OS visitor never saw any of the v3 work. Fixed default (not removed light — toggle still works) in TWO places that must stay in sync: `mobsf/MobSF/templatetags/mi_theme.py` (`_BOOTSTRAP_JS`, the no-flicker inline script) and `mobsf/static/mobinspect/js/theme.js` (`get()`). Both `localStorage.getItem(KEY) || 'system'` → `|| 'dark'`. Added 2 permanent Playwright regression tests in `tests_ui/test_smoke.py` — critically using `page.evaluate("localStorage.removeItem('mi-theme')")` before checking (NOT reusing the session `storage_state` fixture, which would hide this exact bug by having a stale cached value). One test-writing bug caught by the test itself: assumed wrong cycle order (`light→dark→system`) — starting from the new `dark` default, one click lands on `system` next, not `light` (dark sits between light and system in the array) — fixed the test's expected order, not the app.

User then shared `screencapture-behance.png` (project root) — the full Behance case-study page (3360×28800px) for the SAME "CyberGuard" project the 7 dashboard-design images came from (https://www.behance.net/gallery/243300915/). Cropped key sections at full res via `sips -c H W --cropOffset Y 0` (no ImageMagick/PIL available; `sips` — macOS native — supports offset crop). Two concrete new findings beyond the original 7-image catalog:
  1. **Exact typeface confirmed: Albert Sans** (Regular + Medium weights shown in the style-guide specimen) — a real Google Font, SIL OFL licensed, free commercial use. Resolved the "approximated with Inter" gap flagged in the earlier design-audit response.
  2. Brand-application mockups (billboard, app-icon-on-homescreen, storefront signage) + mobile "Smart Incident Response Timeline" — confirms the timeline component genuinely reorients (horizontal desktop → vertical list mobile), but no new UI component types beyond the original catalog.

**Implemented Albert Sans** (user explicitly approved the download first, per file-download permission policy): fetched official woff2 via `fonts.googleapis.com/css2?family=Albert+Sans` → `fonts.gstatic.com` (curl, standard self-hosting extraction, same pattern as existing Inter/JetBrains Mono), saved to `mobsf/static/mobinspect/fonts/AlbertSans-Variable.woff2` (32KB, weight range 400-700). Added `@font-face` in `app.css` (`@layer base`, matches existing Inter/JetBrains Mono pattern). **Scoped to display/headings only** (text-display, h1-h4) — Inter stays for body/small/tiny (legibility at small sizes > display personality). Discovered + documented a real Tailwind v3 limitation: the `fontSize` theme array's options object (`[size, {lineHeight, letterSpacing, fontWeight}]`) does NOT support a `fontFamily` key — verified empirically (silently dropped from compiled output) before committing to a workaround, rather than trusting docs/memory. Fix: explicit override rule in `app.css`'s existing `@layer utilities` block: `.text-display, .text-h1, .text-h2, .text-h3, .text-h4 { font-family: 'Albert Sans', theme('fontFamily.sans'); }` — placed in the utilities layer so it correctly wins over Tailwind's auto-generated same-specificity utility. (Note for next verification: `grep -c` on minified CSS undercounts — minified output is one giant line, so `-c` reports matching *lines* not occurrences; use `grep -o` for real occurrence counts.)

Verified: Django check clean, font file serves 200, full 55/55 Playwright suite passes after both the theme-default change and the font change (each touches every page, so both got full-suite reruns, not spot checks).

**PHASE 8 — literal mirror of home dashboard + sidebar/footer, 2026-07-09 (same day, later still):** user escalated 3x ("mirror copy" → "same means same, still not visible same") — stopped hedging on "keep blue brand, glass only on hero." Delivered:
  - **Sidebar**: icon-only BY DEFAULT now (was: expanded by default, collapse-on-click). Added real `localStorage` persistence (`mi-sidebar` key) in `base/app.html`'s `x-data` + the toggle's `@click` in `components/sidebar.html` — without this, "expand" would silently snap back on every page nav (full-page-reload app, no prior persistence existed). Added a real bottom action group (theme toggle + sign-out form) mirroring CyberGuard's settings/logout/theme cluster — skipped a literal "Settings" icon since `Integrations` nav item already uses the gear icon for that role (no duplicate/fake page).
  - **Real bug found+fixed**: two independent theme-toggle Alpine components (topbar + new sidebar one) don't auto-sync — clicking one left the other showing a stale icon until reload. `theme.js`'s `apply()` already dispatches `mi:theme-change` on `window` (previously unused) — wired `@mi:theme-change.window="pref = $event.detail.value"` on both buttons. Added `tests_ui/test_smoke.py::test_both_theme_toggles_stay_in_sync` (clicks one, asserts BOTH title attrs update without a reload).
  - **Glassmorphism**: added a reusable `.mi-glass` utility in `base/app.html` #mi-chrome (`background: rgb(var(--surface-1)/0.6) !important; backdrop-filter: blur(16px) saturate(150%)` + a 1px gradient-border highlight via mask-composite, + `@supports not` fallback). Applied to EVERY card on `general/home.html` (stat tiles, chart trio, quick actions, recent-activity table, upload dropzone) — not just the hero anymore, and NOT app-wide (dense tables/forms elsewhere still opaque on purpose).
  - **Color**: home.html's icon chips/accent bars switched from blue (`#2563EB`) to the `chart-amber`/`chart-violet` tokens; ambient glow blobs recolored orange/violet; dropzone drag-state recolored amber.
  - **CTA buttons**: new page-scoped `.mi-pillbtn` class (dark `#17181C` pill + the same subtle SVG-noise texture already used by `.btn-primary`, NOT reusing/mutating `.btn-primary` itself) — mirrors CyberGuard's neutral dark "Check Alerts" pill exactly, applied ONLY on `home.html`'s CTAs (Recent scans / Integrations / Choose file). App-wide buttons elsewhere stay on the blue brand — this was a deliberate scope call (home page = literal mirror target; rest of the app keeps its identity) since the user's ask was specifically "for dashboard."
  - **Stat-tile anatomy rewrite**: was icon-chip-top → number → label-below; now matches reference exactly: icon+label inline (small, muted) on row 1, then big number + optional real delta pill on row 2 (same row). Added ONE new real metric to back this honestly: `mobsf/MobSF/views/home.py` now computes `stats['week_delta_pct']` — real week-over-week comparison (`prev_week_ago` to `week_ago` window, mirrors Analytics' `scans_prev_week` pattern exactly), `None` when there's no prior-week data to compare against (never a fabricated 0%/percentage). Only the "Scanned this week" tile shows a delta pill — Android/iOS/Total tiles have no real period-over-period comparison available, so they correctly show NO pill rather than inventing one.
  - Two backend tests updated (existing exact-dict-equality + exact-key-set assertions needed the new `week_delta_pct` key) + **two new tests added** exercising the real (non-None) branch with hand-computed expected values (`test_week_delta_pct_computed_against_real_prior_week` = +100%, `test_week_delta_pct_negative_when_activity_drops` = -75%) — caught+fixed a checksum-collision/length bug in my own first draft of these tests (MD5 field is `varchar(32)`, `f'h{d}'` with 2-digit `d` overflowed) via actually running them, not just writing them.
  - One Playwright test's selector updated (`p.mi-num` → `.mi-num`, tag-agnostic) since the "this week" tile's number moved from a `<p>` to a `<span>` inside a flex row (needed for the inline delta pill) — a genuine, deliberate DOM change, fixed the test to match it.
  - Verified: 47/47 templates compile, 56/56 Playwright pass, 54/54 backend pass (`test_cov_home.py` + `test_cov_home_stats.py`), Django check clean — all after every change in this phase, not spot-checked.

**PHASE 9 — app-wide mirror extension, 2026-07-09 (same day, still later):** user provided `current-design/{1,2}.png` (real screenshots of the LIVE app) — image 1 (Dashboard) confirmed Phase 8 work rendering correctly; image 2 (Recent Scans, a DIFFERENT page) showed the OLD blue design, exposing that the amber/violet/glass/dark-pill mirror was scoped to home.html only and every other page snaps back to blue on navigation. User confirmed: extend app-wide.
  - Promoted `.mi-pillbtn` (dark grain-pill CTA) from home.html-local `<style>` to global `base/app.html` `#mi-chrome` (was duplicated-per-page risk otherwise) — removed the now-dead local copy from home.html, verified 56/56 Playwright still pass after the move.
  - Captured baselines (`$CLAUDE_JOB_DIR/tmp/baseline_mirror`) for 47 files: the same 46-file v3-target list minus home.html, PLUS `appsec_dashboard.html` added back in (it was excluded from the v3 list as the hand-built gauge flagship, but needed the color/glass pass too).
  - Launched workflow `app-wide-mirror-sweep` (run `wf_654c5363-1c1`) — same pipeline pattern as prior phases (design agent → adversarial verify agent, baseline diff). Prompt is explicit and narrow: (1) add `mi-glass` alongside every `card` class, (2) swap ONLY non-semantic blue brand accents (icon-chips, stat-tile accent bars, plain `--mi-accent` values) to `chart-amber`/`chart-violet`, with a HARD exclusion list (severity badges, severity tokens, grade A/B/C/F, the score-gauge's red/orange/blue/green tier logic) that verify agents specifically check the diff against, (3) pill-ify ONLY the page's single primary CTA (not every button), (4) real-data-only — most pages should add zero new widgets, that's an expected/correct outcome, not a shortfall. Verify agents told: a clean no-diff is a valid pass if the page genuinely had nothing to mirror.
  - STATUS: running in background at hand-off. On completion: rebuild CSS, restart, compile-check all 47+home, full Playwright suite, spot-check that severity colors truly survived (this is the failure mode most likely to slip through — a design agent "helpfully" recoloring a severity badge amber because it looked like a generic accent).

Everything still UNCOMMITTED on `feature/granite-llm-integration`; git push remains the user's to run (classifier blocks me).
