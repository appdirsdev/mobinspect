[Service]
# secrets moved out of the world-readable unit into a 0600 root-only file
EnvironmentFile=/etc/mobinspect/secrets.env
# now that nginx terminates TLS, let Django emit Secure cookies + HSTS
Environment=MOBINSPECT_BEHIND_TLS=1
